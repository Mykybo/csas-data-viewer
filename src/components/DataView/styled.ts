import styled from 'styled-components'
import { key } from 'styled-theme'

export const TableWrapper: any = styled.div`
  display: table;
  border: 1px solid ${key('colors.border')};
  font-family: Arial, Helvetica, sans-serif;
  width: 100%;
`
